import React from 'react';

// Por ahora es una función simple. 
// En el futuro le agregaremos props para cambiar el texto, el color, etc.
const Button = () => {
  return (
    <button>
      Soy un Botón
    </button>
  );
};

export default Button;